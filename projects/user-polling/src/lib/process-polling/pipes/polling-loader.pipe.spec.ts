import { PollingLoaderPipe } from './polling-loader.pipe';

describe('PollingLoaderPipe', () => {
  it('create an instance', () => {
    const pipe = new PollingLoaderPipe();
    expect(pipe).toBeTruthy();
  });
});
